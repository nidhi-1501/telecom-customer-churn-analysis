[project]
name = "telecom-customer-churn-analysis"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "dash==4.4.0",
    "pandas",
    "numpy",
    "plotly==6.3.0"
]

[tool.vercel]
entrypoint = "app:server"